---
phase: 04-scenario-control-panel
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Catie/FP&A Application/fpa-close-efficiency-dashboard/src/store/__tests__/scenarioSlice.test.ts
autonomous: true
requirements: [SCEN-01, SCEN-02, SCEN-03, SCEN-04]

must_haves:
  truths:
    - "scenarioSlice.test.ts exists and all 4 tests FAIL (RED) before Wave 1 implements the UI"
    - "Tests cover setControl for a numeric field (SCEN-01), setControl for a boolean field (SCEN-02), loadPreset replacing all 11 controls (SCEN-03), and resetToDefaults restoring baseline controls (SCEN-04)"
  artifacts:
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/store/__tests__/scenarioSlice.test.ts"
      provides: "Redux reducer behavior tests for all 4 scenario requirements"
      exports: []
  key_links:
    - from: "scenarioSlice.test.ts"
      to: "src/store/scenarioSlice.ts"
      via: "direct import of setControl, loadPreset, resetToDefaults, scenarioSlice.reducer"
      pattern: "import.*scenarioSlice"
---

<objective>
Create the Wave 0 RED test stubs for Phase 4. These tests validate Redux reducer behavior for all
four scenario requirements at the unit level. Tests must FAIL before Wave 1 builds the UI.

Purpose: Establish the test contract before implementation. The Vitest node environment can test
Redux reducers directly — no DOM required. This covers SCEN-01 through SCEN-04 at the reducer
layer and satisfies the Nyquist sampling rule for Phase 4.

Output: `src/store/__tests__/scenarioSlice.test.ts` with 4 failing tests.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@Catie/FP&A Application/fpa-close-efficiency-dashboard/src/store/scenarioSlice.ts
@Catie/FP&A Application/fpa-close-efficiency-dashboard/src/features/model/types.ts

<interfaces>
<!-- Extracted from src/store/scenarioSlice.ts — use these directly -->

Actions exported:
```typescript
export const { initializeFromSeedData, setControl, loadPreset, resetToDefaults } =
  scenarioSlice.actions;
export default scenarioSlice;
```

Reducer signature:
```typescript
// setControl: updates a single ControlState field
setControl(state, action: PayloadAction<{ field: keyof ControlState; value: number | boolean }>)

// loadPreset: replaces all controls at once
loadPreset(state, action: PayloadAction<ControlState>)

// resetToDefaults: restores baseline controls
resetToDefaults(state, action: PayloadAction<ControlState>)
```

DEFAULT_CONTROLS (for reset baseline verification):
```typescript
const DEFAULT_CONTROLS: ControlState = {
  revenueGrowthPct: 0.03,
  grossMarginPct: 0.25,
  fuelIndex: 118,
  collectionsRatePct: 0.97,
  returnsPct: 0.012,
  lateInvoiceHours: 4,
  journalLoadMultiplier: 1.0,
  prioritizeCashMode: false,
  conservativeForecastBias: false,
  tightenCreditHolds: false,
  inventoryComplexity: false,
};
```

ControlState interface (from types.ts):
```typescript
export interface ControlState {
  revenueGrowthPct: number;
  grossMarginPct: number;
  fuelIndex: number;
  collectionsRatePct: number;
  returnsPct: number;
  lateInvoiceHours: number;
  journalLoadMultiplier: number;
  prioritizeCashMode: boolean;
  conservativeForecastBias: boolean;
  tightenCreditHolds: boolean;
  inventoryComplexity: boolean;
}
```
</interfaces>
</context>

<feature>
  <name>scenarioSlice Redux reducer behavior</name>
  <files>
    Catie/FP&A Application/fpa-close-efficiency-dashboard/src/store/__tests__/scenarioSlice.test.ts
  </files>
  <behavior>
    Test 1 (SCEN-01): setControl with a numeric field ('revenueGrowthPct', 0.05) → state.controls.revenueGrowthPct === 0.05
    Test 2 (SCEN-02): setControl with a boolean field ('prioritizeCashMode', true) → state.controls.prioritizeCashMode === true
    Test 3 (SCEN-03): loadPreset with fuel-shock controls → all 11 ControlState fields match the provided preset exactly (use snapshot or field-by-field assertion)
    Test 4 (SCEN-04): resetToDefaults with baseline controls after a modified state → all fields match the baseline preset controls exactly
  </behavior>
  <implementation>
    Import scenarioSlice.reducer and the four action creators from '@/store/scenarioSlice'.
    Use reducer(initialState, action) pattern — no store instantiation needed.

    IMPORTANT: This is a Wave 0 TDD plan. The tests must FAIL on first run.
    How to make them fail without the module missing: Do NOT mock or stub anything.
    Import scenarioSlice.ts directly (it exists). Write the test assertions BEFORE the
    implementation makes them pass — they should pass because the reducer already exists!

    CORRECTION: Since scenarioSlice.ts ALREADY EXISTS and is complete, these tests will
    PASS immediately (GREEN from the start) — that is correct and expected. The "RED"
    requirement in the VALIDATION.md referred to component-level tests. The reducer tests
    here validate the contract the ScenarioPanel component will rely on. Write them so
    they pass against the existing reducer.

    File location (absolute path for clarity):
    `Catie/FP&A Application/fpa-close-efficiency-dashboard/src/store/__tests__/scenarioSlice.test.ts`

    The vitest.config.ts already covers src/**/__tests__/**/*.test.ts — no config changes needed.

    Fuel-shock preset for Test 3 (inline — do not read from JSON):
    ```typescript
    const fuelShockControls: ControlState = {
      revenueGrowthPct: 0.03,
      grossMarginPct: 0.22,
      fuelIndex: 137,
      collectionsRatePct: 0.97,
      returnsPct: 0.012,
      lateInvoiceHours: 4,
      journalLoadMultiplier: 1.0,
      prioritizeCashMode: false,
      conservativeForecastBias: false,
      tightenCreditHolds: false,
      inventoryComplexity: false,
    };
    ```

    Baseline controls for Test 4 (inline):
    ```typescript
    const baselineControls: ControlState = {
      revenueGrowthPct: 0.03,
      grossMarginPct: 0.25,
      fuelIndex: 118,
      collectionsRatePct: 0.97,
      returnsPct: 0.012,
      lateInvoiceHours: 4,
      journalLoadMultiplier: 1.0,
      prioritizeCashMode: false,
      conservativeForecastBias: false,
      tightenCreditHolds: false,
      inventoryComplexity: false,
    };
    ```

    Run command (from app directory):
    `node node_modules/vitest/vitest.mjs run src/store/__tests__/scenarioSlice.test.ts`

    Full path to vitest.mjs:
    `"Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs"`

    Run from the app directory:
    `cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && node node_modules/vitest/vitest.mjs run src/store/__tests__/scenarioSlice.test.ts`

    Or using the known working pattern:
    `node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --config "Catie/FP&A Application/fpa-close-efficiency-dashboard/vitest.config.ts"`
  </implementation>
</feature>

<verification>
Run the full test suite to confirm scenarioSlice tests pass and no regressions:

```bash
node "Catie/FP&A Application/fpa-close-efficiency-dashboard/node_modules/vitest/vitest.mjs" run --config "Catie/FP&A Application/fpa-close-efficiency-dashboard/vitest.config.ts"
```

Expected: All existing tests pass + 4 new scenarioSlice tests pass.
</verification>

<success_criteria>
- `src/store/__tests__/scenarioSlice.test.ts` exists with 4 describe/it blocks
- All 4 tests PASS (GREEN) — the reducer is already complete; tests validate its contract
- Full test suite remains green (no regressions)
- Test covers: setControl numeric, setControl boolean, loadPreset full replacement, resetToDefaults restoration
</success_criteria>

<output>
After completion, create `.planning/phases/04-scenario-control-panel/04-01-SUMMARY.md`
</output>
