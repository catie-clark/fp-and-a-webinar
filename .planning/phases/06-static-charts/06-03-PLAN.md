---
phase: 06-static-charts
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified: []
autonomous: false
requirements:
  - CHRT-02
  - CHRT-03
  - CHRT-04

must_haves:
  truths:
    - "All 3 charts render visually in the browser with correct data and colors"
    - "No SSR hydration errors in browser DevTools console on page load"
    - "Cash Flow show/hide toggle collapses and restores the panel"
    - "AR Aging 90+ stat matches ar90Ratio from data (10–12%)"
  artifacts:
    - path: "Catie/FP&A Application/fpa-close-efficiency-dashboard/src/components/dashboard/ChartsSection/ChartsSection.tsx"
      provides: "Verified working charts section — human confirmed"
  key_links:
    - from: "browser console"
      to: "DashboardApp.tsx / ChartsSection.tsx"
      via: "DevTools network + console — zero errors on load"
      pattern: "zero hydration errors"
---

<objective>
Human verification that all three static charts render correctly in the browser. Confirms chart colors, data accuracy, SSR hydration safety, and interactive toggle behavior.

Purpose: Automated tests cannot verify visual rendering, chart colors, or browser hydration errors. A live browser check is the quality gate before this phase is marked complete.
Output: Confirmed working charts — phase ready for verify-work.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-static-charts/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and confirm no build errors</name>
  <files></files>
  <action>
    Start the Next.js dev server and verify the app loads cleanly.

    Run in the app directory:
    ```
    cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && npm run dev
    ```

    Wait for "Ready" message. Then in a separate terminal verify TypeScript:
    ```
    cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && npx tsc --noEmit
    ```

    Expected: zero TypeScript errors, dev server starts on localhost:3000.
  </action>
  <verify>
    <automated>cd "Catie/FP&A Application/fpa-close-efficiency-dashboard" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>tsc --noEmit exits clean (no errors). Dev server starts without compilation errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — all 3 charts render correctly with no SSR errors</name>
  <files></files>
  <action>
    Pause for human verification. The executor opens the browser and confirms all three charts render and behave correctly per the how-to-verify checklist. No code changes are needed — this is a visual and interactive QA check only.
  </action>
  <what-built>
    Three static Recharts charts wired into the dashboard below the Close Stage Tracker:
    - Pipeline to Invoiced: teal BarChart with 5 CRM stages (Qualified to Invoiced), currency Y-axis
    - AR Aging: horizontal stacked bar with 5 color-coded buckets + "X% aged 90+ days" stat + color legend
    - 13-Week Cash Flow: area/line chart with solid actuals (W1-W6) + dashed forecast (W7-W13) + Hide/Show toggle button
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 and check the following:

    CHARTS PRESENT:
    1. Scroll below the Close Stage Tracker — confirm you see a 2-column row (Pipeline + AR Aging) followed by a full-width Cash Flow section
    2. All three chart areas have visible content (not blank white boxes or error messages)

    PIPELINE TO INVOICED (left of top row):
    3. 5 bars visible — Qualified, Proposal, Negotiation, Closed Won, Invoiced
    4. Bars are TEAL (#05AB8C), not Recharts default blue
    5. Y-axis shows currency values (e.g., "$500K", "$1.0M")
    6. Hover over any bar — tooltip shows "Total:" and "Weighted:" values

    AR AGING (right of top row):
    7. Single horizontal stacked bar visible spanning full card width
    8. Bar shows clear color progression: green (left/Current) to red (right/90+)
    9. Stat in top-right shows a percentage like "11.0% aged 90+ days" in red
    10. Color legend with 5 labels (Current, 1-30 days, 31-60 days, 61-90 days, 90+ days) visible below the bar

    13-WEEK CASH FLOW (full-width bottom row):
    11. Chart shows a line/area with a SOLID portion (W1-W6, actuals) and a DASHED portion (W7-W13, forecast)
    12. Visual distinction between solid and dashed is clear at presenter-demo viewing distance
    13. Hover over a point — tooltip shows week label, Actual/Forecast label, inflow, outflow, and net cash values
    14. Click "Hide" button in the Cash Flow panel header — chart collapses, "Show" button appears
    15. Click "Show" — chart returns

    NO ERRORS:
    16. Open DevTools Console (F12) — confirm ZERO red errors on page load
    17. No "Warning: Expected server HTML to contain a matching..." hydration errors
    18. No "window is not defined" errors

    THEME CHECK (optional but recommended):
    19. If dark mode is available, toggle — verify charts remain visible (bars and lines not invisible against dark background)
  </how-to-verify>
  <verify>
    <automated>MISSING — visual and interactive QA requires human browser verification; no automated equivalent</automated>
  </verify>
  <done>Human confirms all 19 checks pass with "approved" signal.</done>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe any issues found (e.g., "Pipeline bars are blue not teal", "Cash Flow blank white box", "Console shows hydration error").
  </resume-signal>
</task>

</tasks>

<verification>
Phase 6 is complete when the human-verify checkpoint is approved with all 19 checks passing.
</verification>

<success_criteria>
- All 3 charts render visually with correct colors and data
- Zero browser console errors on page load
- Cash Flow toggle show/hide works
- AR Aging 90+ ratio stat visible and accurate (10-12%)
- Dark mode (if tested) shows readable charts
</success_criteria>

<output>
After completion, create `.planning/phases/06-static-charts/06-03-SUMMARY.md`
</output>
