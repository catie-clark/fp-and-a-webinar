---
phase: 07-reactive-margin-bridge
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified: []
autonomous: false
requirements:
  - CHRT-01
must_haves:
  truths:
    - "Margin Bridge chart renders 6 bars with correct colors in the browser"
    - "Moving Revenue Growth or Gross Margin slider causes bars to animate within 1 second"
    - "Dark mode theme toggle shows total bars (Baseline EBITDA, Adjusted EBITDA) remaining visible"
    - "Amber glow is visible on the card border when any EBITDA-affecting slider moves"
    - "No other dashboard component flickers or re-renders noticeably when Margin Bridge updates"
    - "Fuel Cost Shock preset causes Fuel Index bar to turn red and extend below zero line"
  artifacts:
    - path: "src/components/dashboard/MarginBridgeSection/MarginBridgeSection.tsx"
      provides: "Verified working in browser — chart animates, glow fires, dark mode correct"
  key_links:
    - from: "browser slider interaction"
      to: "MarginBridgeSection bar heights"
      via: "Redux dispatch → useSelector re-render → Recharts animation"
      pattern: "visual QA only"
---

<objective>
Start the development server and perform browser QA to confirm the Margin Bridge chart works end-to-end in both light and dark themes.

Purpose: The chart's visual correctness — bar colors, animations, dark mode visibility, amber glow — cannot be verified by automated tests. This checkpoint gates Phase 7 completion on a human confirming the "wow moment" works before the webinar.

Output: Human sign-off that CHRT-01 is met. No code changes expected — this is a verification-only plan.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-reactive-margin-bridge/07-03-SUMMARY.md
@.planning/phases/07-reactive-margin-bridge/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and verify full test suite is GREEN</name>
  <files></files>
  <action>
    1. Run the full test suite to confirm all tests pass:

    ```bash
    cd "C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/FP&A Webinar/Catie/FP&A Application/fpa-close-efficiency-dashboard" && node "C:/Users/RachurA/AppData/Local/node_modules/vitest/vitest.mjs" run --reporter=verbose 2>&1
    ```

    2. If all tests GREEN, start the development server:

    ```bash
    cd "C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/FP&A Webinar/Catie/FP&A Application/fpa-close-efficiency-dashboard" && npm run dev
    ```

    3. Open http://localhost:3000 in the browser.

    4. Report back with confirmation that the dev server started without errors.
  </action>
  <verify>
    <automated>cd "C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/FP&A Webinar/Catie/FP&A Application/fpa-close-efficiency-dashboard" && node "C:/Users/RachurA/AppData/Local/node_modules/vitest/vitest.mjs" run --reporter=verbose 2>&1</automated>
  </verify>
  <done>
    All tests GREEN. Dev server running at localhost:3000 without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    MarginBridgeSection — a full-width reactive waterfall chart inserted between the Close Tracker and the static charts in the dashboard. The chart shows 6 bars: Baseline EBITDA (indigo), Revenue Growth Impact (gold/red), Gross Margin Impact (gold/red), Fuel Index Impact (gold/red), All Other Levers (gold/red), and Adjusted EBITDA (indigo). Bars animate on slider movement. Card border glows amber when Adjusted EBITDA changes. Dark mode uses a lighter indigo for the total bars.
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 in your browser and verify each item below.

    **Visual Layout**
    1. Confirm the Margin Bridge card appears between the Close Tracker section and the Pipeline/AR Aging/Cash Flow charts.
    2. Confirm the card has the "Margin Bridge" title on the left and "Adjusted EBITDA: $X.XM" on the right of the header.
    3. Confirm 6 bars are visible on the chart.

    **Bar Colors (Light Mode)**
    4. Confirm bars 1 and 6 (Baseline EBITDA, Adjusted EBITDA) are dark indigo.
    5. Confirm bars 2–5 (delta bars) are gold (#F5A800) at default controls — at default all deltas are 0 so bars render at zero height. This is expected behavior.

    **Reactivity — Revenue Growth Slider**
    6. Move the Revenue Growth slider to +5%. Confirm:
       - The Revenue Growth bar rises (gold, positive).
       - The Adjusted EBITDA bar height increases.
       - The "Adjusted EBITDA: $X.XM" header value updates.
       - Bar animation is smooth (~300ms), not instant or jarring.
       - The amber glow fires briefly on the card border.

    **Reactivity — Fuel Cost Shock Preset**
    7. Select the "Fuel Cost Shock" preset from the scenario dropdown. Confirm:
       - The Fuel Index bar turns red (#E5376B) and drops below the zero line.
       - The Adjusted EBITDA bar is noticeably shorter than Baseline EBITDA.
       - The header shows a lower "Adjusted EBITDA" value.

    **Dark Mode**
    8. Toggle dark mode (OS system preference or the dark mode toggle in the app). Confirm:
       - The Baseline EBITDA and Adjusted EBITDA bars remain clearly visible (lighter blue-indigo, not invisible).
       - Gold and red bars are still visible against the dark card background.
       - Bar labels are readable.

    **No Flicker / Isolation**
    9. Move any slider slowly. Confirm that KPI cards, Close Tracker, and the static charts below do NOT visibly flash or re-render. Only the Margin Bridge chart should update.

    **Amber Glow**
    10. Reset to defaults. Move the Gross Margin slider. Confirm the card border briefly shows an amber glow. (It appears and fades within ~750ms — watch for it immediately after moving the slider.)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 10 items pass. Or describe any issues found (e.g., "dark mode bars invisible", "glow not firing", "bars not animating") so they can be fixed.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 7 is complete when:
- [ ] All 7 marginBridge.test.ts tests GREEN
- [ ] Full test suite GREEN (no regressions)
- [ ] tsc --noEmit passes
- [ ] Human has typed "approved" for all 10 visual QA items
</verification>

<success_criteria>
CHRT-01 met:
- Margin Bridge chart renders as a Recharts BarChart with gold (#F5A800) bars, a ReferenceLine at zero, and currency-formatted tooltips
- Moving the Revenue Growth or Gross Margin slider causes the Margin Bridge chart to visually update within one second without causing other dashboard components to flicker or re-render noticeably
- The chart renders correctly in both light and dark themes without any bar or label becoming invisible
</success_criteria>

<output>
After completion, create `.planning/phases/07-reactive-margin-bridge/07-04-SUMMARY.md`

Also update:
- `.planning/STATE.md` — mark Phase 7 complete, set current focus to Phase 8
- `.planning/ROADMAP.md` — mark Phase 7 ✅ with completion date
</output>
